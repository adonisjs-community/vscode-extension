class HomeController {
  /**
   * Store a new home object.
   *
   * @param {*} context
   */
  store(context) {
    // anything
  }

  /**
   * Delete a new home object.
   *
   * @param {*} context
   */
  delete(context) {
    // anything
  }
}

module.exports = HomeController;
